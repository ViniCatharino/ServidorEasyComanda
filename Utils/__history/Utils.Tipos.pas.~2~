unit Utils.Tipos;

interface

type
  TStatusMesa   = (tsmAberta, tsmFechada, tsmLivre, tsmReservada);
  TStatusPedido = (tspAberto, tspEntregue);

var
  StatusMesaArray   : array[0..3] of string = ('A', 'F', 'L', 'R');
  StatusPedidoArray : array[0..1] of string = ('A', 'E');

implementation


end.
